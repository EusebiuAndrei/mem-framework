import { QueryMethod } from '../types';
import ActionHandler from './ActionHandler';

class QueryHandler extends ActionHandler<QueryMethod> {
  constructor() {
    super();
  }

  // Implement caching
}

export default QueryHandler;
